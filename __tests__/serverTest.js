import server from '../server/server';
import * as request from 'supertest';

describe('server router integration', () => {
  describe('/refresh', () => {});
  describe('/connect', () => {});
  describe('/newSchema', () => {});
  describe('/results', () => {});
});
